/**
 * AuthPage — delegates to NovaLoginPage which integrates
 * the Nova animation video with the real ShopForge login/register form.
 */
import NovaLoginPage from '../components/auth/NovaLoginPage';

export default function AuthPage() {
  return <NovaLoginPage />;
}
